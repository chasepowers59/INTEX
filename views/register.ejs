<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register | Ella Rises</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, var(--col-cream) 0%, #fff 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .register-container {
            width: 100%;
            max-width: 450px;
            padding: 2rem;
        }

        .register-card {
            background: white;
            padding: 3rem;
            border-radius: 30px;
            box-shadow: 0 20px 60px rgba(148, 170, 185, 0.2);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .register-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 8px;
            background: linear-gradient(90deg, var(--col-blue), var(--col-purple), var(--col-green));
        }

        .brand-logo {
            font-family: 'DM Serif Display', serif;
            font-size: 2.5rem;
            color: var(--col-dark-gray);
            margin-bottom: 0.5rem;
        }

        .brand-tagline {
            font-family: 'Montserrat', sans-serif;
            font-size: 0.9rem;
            color: var(--text-light);
            margin-bottom: 2.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .form-control {
            background: #f8f9fa;
            border: 2px solid transparent;
            transition: all 0.3s ease;
            padding: 1.2rem;
            font-size: 1rem;
        }

        .form-control:focus {
            background: white;
            border-color: var(--col-blue);
            box-shadow: 0 5px 15px rgba(148, 170, 185, 0.1);
        }

        .btn-register {
            background: var(--col-dark-gray);
            color: white;
            width: 100%;
            padding: 1.2rem;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 1rem;
            transition: all 0.3s ease;
            border: none;
            border-radius: 8px;
            cursor: pointer;
        }

        .btn-register:hover {
            background: var(--col-blue);
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(148, 170, 185, 0.3);
        }

        .back-link {
            display: inline-block;
            margin-top: 2rem;
            font-size: 0.9rem;
            color: #aaa;
            text-decoration: none;
        }

        .back-link:hover {
            color: var(--col-dark-gray);
        }

        .login-link {
            margin-top: 1.5rem;
            font-size: 0.9rem;
            color: #666;
        }

        .login-link a {
            color: var(--col-blue);
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="register-container">
        <div class="register-card">
            <div class="brand-logo">Ella Rises</div>
            <div class="brand-tagline">Create Account</div>

            <% if (error) { %>
                <div
                    style="background: #ffebee; color: #c62828; padding: 1rem; border-radius: 10px; margin-bottom: 1.5rem; font-size: 0.9rem;">
                    <i class="fas fa-exclamation-circle"></i>
                    <%= error %>
                </div>
            <% } %>

            <% if (messages && messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success" role="alert" style="margin-bottom: 1.5rem;">
                    <%= messages.success[0] %>
                </div>
            <% } %>

            <form action="/auth/register" method="POST">
                <div class="mb-3" style="text-align: left;">
                    <label for="participant_first_name" class="form-label"
                        style="font-weight: 600; font-size: 0.85rem; color: #666; margin-bottom: 0.5rem; display: block; text-transform: uppercase;">First
                        Name</label>
                    <input type="text" id="participant_first_name" name="participant_first_name" class="form-control"
                        placeholder="Enter your first name" required>
                </div>

                <div class="mb-3" style="text-align: left;">
                    <label for="participant_last_name" class="form-label"
                        style="font-weight: 600; font-size: 0.85rem; color: #666; margin-bottom: 0.5rem; display: block; text-transform: uppercase;">Last
                        Name</label>
                    <input type="text" id="participant_last_name" name="participant_last_name" class="form-control"
                        placeholder="Enter your last name" required>
                </div>

                <div class="mb-3" style="text-align: left;">
                    <label for="participant_email" class="form-label"
                        style="font-weight: 600; font-size: 0.85rem; color: #666; margin-bottom: 0.5rem; display: block; text-transform: uppercase;">Email</label>
                    <input type="email" id="participant_email" name="participant_email" class="form-control"
                        placeholder="Enter your email" required>
                </div>

                <div class="mb-3" style="text-align: left;">
                    <label for="participant_password" class="form-label"
                        style="font-weight: 600; font-size: 0.85rem; color: #666; margin-bottom: 0.5rem; display: block; text-transform: uppercase;">Password</label>
                    <input type="password" id="participant_password" name="participant_password" class="form-control"
                        placeholder="••••••••" required>
                </div>

                <div class="mb-3" style="text-align: left;">
                    <label for="confirm_password" class="form-label"
                        style="font-weight: 600; font-size: 0.85rem; color: #666; margin-bottom: 0.5rem; display: block; text-transform: uppercase;">Confirm
                        Password</label>
                    <input type="password" id="confirm_password" name="confirm_password" class="form-control"
                        placeholder="••••••••" required>
                </div>

                <button type="submit" class="btn btn-primary btn-register">Create Account</button>
            </form>

            <div class="mt-3" style="font-size: 0.9rem; color: #666;">
                Already have an account? <a href="/auth/login" style="color: var(--col-blue); font-weight: 600; text-decoration: none;">Login</a>
            </div>

            <a href="/" class="back-link">← Back to Website</a>
        </div>
    </div>

</body>

</html>

