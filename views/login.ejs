<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Ella Rises</title>
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/img/Ella Rises Logo (2).PNG">
    <link rel="shortcut icon" type="image/png" href="/img/Ella Rises Logo (2).PNG">
    <link rel="apple-touch-icon" href="/img/Ella Rises Logo (2).PNG">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            min-height: 100vh;
            display: flex;
            overflow: hidden;
        }

        /* Left Side - Image Section */
        .login-left {
            flex: 1;
            background-image: url('/img/ER Summer Summit 2022-165.jpg');
            background-size: cover;
            background-position: center;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem;
        }

        .login-left::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255, 209, 199, 0.7) 0%, rgba(148, 170, 185, 0.6) 100%);
        }

        .login-left::after {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.3);
            z-index: 1;
        }

        .login-left-content {
            position: relative;
            z-index: 2;
            color: white;
            text-align: center;
            max-width: 500px;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        }

        .login-left-content h1 {
            font-family: 'DM Serif Display', serif;
            font-size: 3rem;
            margin-bottom: 1rem;
            color: white;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
        }

        .login-left-content p {
            font-size: 1.2rem;
            line-height: 1.8;
            color: white;
            font-weight: 500;
            text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.5);
        }

        /* Right Side - Login Form */
        .login-right {
            flex: 1;
            background: linear-gradient(135deg, var(--col-blue) 0%, #7A8FA0 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem;
            position: relative;
        }

        .login-right::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 209, 199, 0.1) 0%, transparent 70%);
            animation: pulse 8s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.3; }
        }

        .login-form-container {
            width: 100%;
            max-width: 450px;
            position: relative;
            z-index: 2;
        }

        .login-header {
            margin-bottom: 2.5rem;
        }

        .login-header small {
            display: block;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .login-header h2 {
            font-family: 'DM Serif Display', serif;
            font-size: 2.5rem;
            color: white;
            margin-bottom: 0.5rem;
        }

        .login-header p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1rem;
        }

        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 2.5rem;
            border-radius: 25px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            font-weight: 600;
            font-size: 0.85rem;
            color: var(--col-dark-gray);
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .input-wrapper {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--col-blue);
            font-size: 1.1rem;
        }

        .form-control {
            width: 100%;
            padding: 1rem 1rem 1rem 3rem;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
            font-family: 'Montserrat', sans-serif;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--col-blue);
            box-shadow: 0 0 0 3px rgba(148, 170, 185, 0.2);
        }

        .btn-login {
            width: 100%;
            padding: 1rem;
            background: var(--col-blue);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(148, 170, 185, 0.4);
            font-family: 'Montserrat', sans-serif;
        }

        .btn-login:hover {
            background: var(--col-dark-gray);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(148, 170, 185, 0.5);
        }

        .forgot-password {
            text-align: right;
            margin-top: 0.5rem;
        }

        .forgot-password a {
            color: var(--col-blue);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }

        .register-link {
            text-align: center;
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #e0e0e0;
        }

        .register-link p {
            color: var(--text-light);
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .register-link a {
            color: var(--col-blue);
            text-decoration: none;
            font-weight: 600;
        }

        .register-link a:hover {
            text-decoration: underline;
        }

        .error-message {
            background: #ffebee;
            color: #c62828;
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .success-message {
            background: #e8f5e9;
            color: #2e7d32;
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        /* Responsive Design */
        @media (max-width: 968px) {
            body {
                flex-direction: column;
            }

            .login-left {
                min-height: 40vh;
            }

            .login-right {
                min-height: 60vh;
            }

            .login-left-content h1 {
                font-size: 2rem;
            }

            .login-left-content p {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <!-- Left Side - Image with Overlay -->
    <div class="login-left">
        <div class="login-left-content">
            <a href="/" class="logo-alternative" style="display: flex; justify-content: center; margin-bottom: 1rem;">
                <img src="/img/Ella Rises Logo (2).PNG" alt="Ella Rises Logo" class="login-logo-img">
            </a>
            <h1 style="display: none;">Ella Rises</h1>
            <p>Empowering Latina Youth</p>
            <p style="margin-top: 1rem; font-size: 1rem; font-weight: 400;">
                Inspiring the next generation of leaders through heritage, education, and sisterhood.
            </p>
        </div>
    </div>

    <!-- Right Side - Login Form -->
    <div class="login-right">
        <div class="login-form-container">
            <div class="login-header">
                <small>Login your account</small>
                <h2>Welcome Back!</h2>
                <p>Enter your email and password</p>
            </div>

            <div class="login-card">
                <% if (error) { %>
                    <div class="error-message">
                        <i class="fas fa-exclamation-circle"></i>
                        <span><%= error %></span>
                    </div>
                <% } %>

                <% if (messages && messages.success && messages.success.length > 0) { %>
                    <div class="success-message">
                        <i class="fas fa-check-circle"></i>
                        <span><%= messages.success[0] %></span>
                    </div>
                <% } %>

                <form action="/auth/login" method="POST">
                    <div class="form-group">
                        <label for="participant_email" class="form-label">Email address</label>
                        <div class="input-wrapper">
                            <i class="fas fa-envelope input-icon"></i>
                            <input type="email" id="participant_email" name="participant_email" class="form-control"
                                placeholder="Enter your email" required autocomplete="email">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="participant_password" class="form-label">Password</label>
                        <div class="input-wrapper">
                            <i class="fas fa-lock input-icon"></i>
                            <input type="password" id="participant_password" name="participant_password" class="form-control"
                                placeholder="Enter your password" required autocomplete="current-password">
                        </div>
                    </div>

                    <button type="submit" class="btn-login">Sign in</button>
                </form>

                <div class="register-link">
                    <p>Don't have an account?</p>
                    <a href="/auth/register">Create an account</a>
                </div>
            </div>
        </div>
    </div>

</body>

</html>
