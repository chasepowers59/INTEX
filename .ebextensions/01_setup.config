option_settings:

  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production

container_commands:
  01_migrate:
    command: "npm run migrate"
    leader_only: true
  # 02_seed:
  #   command: "npm run seed"
  #   leader_only: true
  # NOTE: Seed is disabled to prevent data loss on deployments
  # Run seeds manually only when needed: npm run seed
  03_populate_events:
    command: "npm run populate-events"
    leader_only: true
    ignoreErrors: true

